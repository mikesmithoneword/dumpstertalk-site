/* ==========================================================
   SEGMENT IDEA FORM SETTINGS  (Web3Forms)

   The access key below is filled in and the form is live. Empty it out
   and the Segments section falls back to a plain email link instead.

   The key is designed to be public - Web3Forms say so themselves - so it
   is safe sitting in this file in a public repository. It only lets a
   form post a message to the inbox the key belongs to.
   ========================================================== */
window.DT_FORM = {
  // Paste the access key between the quotes
  accessKey: "650d83a1-6634-4229-b564-bb5999fb902e",

  // Where a failed submission tells people to write instead
  fallbackEmail: "info@dumpstertalk.com"
};
